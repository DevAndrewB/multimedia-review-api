# multimedia-review-api

### Run Server <br />
Add DB connection string to config file if not already there <br />
Start API by running: npm run server <br />
Added client proxy pointing to server PORT -> Any new API calls can be made directly to route ('/api/v1/reviews') <br />
