# multimedia-review-api

##Run Server
Add DB connection string to config file if not already there
Start API by running: npm run server
Added client proxy pointing to server PORT -> Any new API calls can be made directly to route ('/api/v1/reviews')
